// Always-visible honesty banner: this is a portfolio demo, not a real shop.
export default function DemoBanner() {
  return (
    <div className="bg-ink text-cream">
      <p className="mx-auto max-w-content px-4 py-1.5 text-center text-xs tracking-wide">
        Demo store · sample data · no real orders or payments are processed
      </p>
    </div>
  );
}
