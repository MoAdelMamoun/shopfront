# Screenshots

These images are referenced by the project `README.md`. They were captured from
the static build (`npm run build` → `out/`) served locally. Re-generate them any
time by rebuilding and re-shooting the served `out/` directory.
